# ВЕТОП Telegram-бот — контекст проекта

Прочитай этот файл целиком перед началом работы: здесь вся история проекта,
договорённости с владельцем и правила работы.

## Кто заказчик и как общаться

- Владелец: **Абдурроууф** (админ, Telegram ID 632294583), общение — **на русском**,
  простым языком без жаргона: он управляет бизнесом, а не программирует.
- Обращаться к нему **«Доктор-шеф»**, ассистента он зовёт **«Джарвис»**
  (его просьба, поддерживать этот стиль).
- Работает **с телефона** (Railway — через мобильное приложение/браузер).
- Стиль работы: обсуждаем идею → он говорит «делай» → реализация → тесты →
  коммит → **PR → сразу мерж в main** (он дал постоянное разрешение сливать самому).
- Ветка выдаётся на каждую сессию своя (`claude/...`), база `main`;
  PR мержить в main сразу самому (разрешение постоянное).

## Бизнес

ОсОО «ВЕТОП» — оптовая торговля ветеринарными препаратами в Кыргызстане,
долговая модель (товар в долг, клиенты гасят частями). Валюта — сом.

- **4 склада**: Бишкек (основной), Кара-Балта, Каракол, Манас.
- **Сотрудники** (STAFF в bot.py): Абдурроууф (админ, Бишкек), Жуми и Бека
  (Бишкек + доступ к Кара-Балте), Данияр (Каракол), Азамат (Манас, ID 6525019701,
  раньше значился как «Аза» — это один человек).
- **3 групповых чата**: Бишкек+Кара-Балта, Каракол, Манас — подключаются
  к ленте операций командой /feed в самом чате.
- **Операции можно писать прямо в чате склада** (21.07.2026): в чате-ленте
  бот обрабатывает текст и голосовые СОТРУДНИКОВ, но реагирует только на
  похожее на операцию (quiet-режим — на разговоры не реагирует, эхо
  «Распознал» не шлёт). Если сообщение прошло фильтр, бот отвечает и на
  неудачу (уточняющий вопрос ИИ, ошибка API) — молчание не должно выглядеть
  как «оплата записана» (ревизия 21.07.2026). Склад не указан → берётся склад чата (если чат привязан к
  одному складу). Операция из чата склада не дублируется лентой в этот же
  чат (exclude_chat_id в post_feed/feed_operation/feed_invoice_pdf).
  Расход API почти нулевой: текст в чате склада идёт в Claude через
  бесплатный фильтр _looks_like_operation (слова-приметы OP_KEYWORDS,
  товары прайса, известные клиенты + цифры) ЛИБО через явное обращение
  «Джарвис, …» (фильтр не нужен). ГОЛОСОВЫЕ в чатах складов (решение
  владельца 21.07.2026, кодовое слово): обрабатываются ТОЛЬКО начинающиеся
  с «Джарвис» (_strip_wake_word: варианты Джарвис/Жарвис/Jarvis + нечёткое
  совпадение первого слова ≥0.7, слово добавлено в подсказку Whisper);
  без обращения — молчание. Обращение ищется бесплатным Groq (без
  GROQ_API_KEY голосовые в группах не слушаются вовсе); если обращение
  услышано и задан ELEVENLABS_API_KEY, аудио повторно распознаётся платным
  ElevenLabs (кыргызский/смешанная речь работают и в группе — квота
  тратится только на обращения к боту), при ошибке ElevenLabs остаёмся на
  тексте Whisper. После обращения бот шлёт эхо «🎤 Распознал: …» и
  обрабатывает без фильтра примет. В личке по-прежнему ElevenLabs для
  всех голосовых (обращение не нужно).

## Архитектура

- `bot.py` — хендлеры Telegram (python-telegram-bot 20.3), диалог с Claude,
  подтверждения кнопками, фоновые задачи (сводки/напоминания/бэкапы).
- `db.py` — SQLite (на Railway Volume `/data/vetop.db`), своя коллация NOCASEU
  для кириллицы, журнал операций с дельтами (отмена = сторно).
  ⚠️ ИНЦИДЕНТ 21.07.2026: после загрузки Каракола вся база оказалась пустой —
  Volume не был подключён к сервису, _db_path() упал в фолбэк «файл рядом
  с кодом», который стирается при каждом деплое (в ту ночь мержилось много
  PR). Данные не потеряны: остатки в karakol_stock_data.py, клиенты — в
  подготовленном сообщении в чате. Защита (PR #47): при старте бот шлёт
  админу тревогу, если хранилище временное; /dbinfo (админ) показывает
  путь/размер/счётчики/статус.
  ✅ РАЗРЕШЕНО 21.07.2026: владелец через ИИ-помощника Railway создал
  Volume `worker-data` (mount path /data), /dbinfo показал «🟢 ПОСТОЯННОЕ»
  (/data/vetop.db). Каракол перезагружен: /fullmode Каракол → /loadkarakol
  (71 позиция, 6490 шт — подтверждено скриншотом /stock) → клиенты с
  долгами (29 шт, 1'447'085 сом — резервный текст сообщения лежит в
  karakol_clients_message.txt в репозитории) → /feed Каракол в группе.
  Если в новом чате что-то «пусто» — первым делом /dbinfo.
- `prices.py` — прайс-лист: SEED_DATA (104 позиции) сидируют таблицу products
  при первом запуске, дальше прайс живёт в базе (set_data перезагружает,
  bot._refresh_price_dependents() пересобирает системный промпт и STT-подсказку).
  Нумерация 21.07.2026 приведена к фирменному прайсу владельца от 10.06.2026
  (жалоба: /stock шёл не в том порядке): ГАБИВИТ/КАЛЬФОТОН/СУРФАГОН теперь
  №58–61 после БУТАСТИМА, бывшие №58–100 сдвинулись на +4. Старые базы
  перенумеровывает одноразовая миграция db._migrate_price_order (флаг
  price_order_v2 в settings, карта prices.RENUMBER_2026_07) — включая журнал
  операций (stock_deltas), поэтому /undo старых операций работает верно.
  Фирменный прайс — единственный источник порядка; новые товары добавлять
  в конец либо согласовывать номер с владельцем.
- `invoice_pdf.py` — PDF: накладная, черновик (водяной знак), возврат, акт
  сверки, прайс-лист. Шрифты DejaVu, зелёный стиль #1b5e20, лого IMG_3248.jpeg.
- `export_xlsx.py` — Excel-экспорт (openpyxl).
- Хостинг: **Railway**, проект `pure-friendship`, сервис `worker`,
  Volume `worker-data` смонтирован в `/data`. Деплой автоматом из main.

## Ключевые решения (не менять без согласования с владельцем)

- **Подтверждение кнопками** перед любой записью в базу (заявки живут 15 мин).
- **Только админ подтверждает**: перемещения между складами, инвентаризации,
  возвраты, сдачу выручки. Приход товара извне — админ и «старшие» (/grant).
- **Новый клиент со стартовым долгом > 0** в реальной накладной сотрудника —
  тоже только через подтверждение админа (защита от «рисования» долгов);
  новый клиент с долгом 0 — сотрудник создаёт сам. Долг существующего
  клиента словами не меняется вообще — только операциями.
- Клиенты и долги **раздельны по складам**; нечёткий поиск имён — только
  внутри склада; новый клиент создаётся только после подтверждения.
- **Сотрудник с несколькими складами** (например, Бека после /access) при
  операции без указания склада получает кнопки «на каком складе провести?»
  (kind pick_wh в dispatch_action) — чтобы не провести случайно на родной
  склад. Админа не спрашиваем. Бека получил доступ к Караколу 21.07.2026
  на время тестирования (/noaccess Бека Каракол — забрать).
- Нехватка на складе: РЕАЛЬНУЮ накладную сверх остатка **провести нельзя**
  (решение владельца 21.07.2026, insufficient_stock — проверка и при
  создании заявки, и на кнопке «Провести»; при замене накладной старые
  количества считаются возвращёнными). Черновики — без ограничений.
  Перемещения/возвраты пока по-старому (минус возможен с предупреждением).
- Каждая операция сотрудника дублируется админу в личку + в ленту чата склада.
  Накладные в ленту идут ОДНИМ сообщением — PDF со сводкой в подписи, без
  отдельного текста (просьба владельца 21.07.2026, было два сообщения).
- Касса сотрудника **выводится из журнала** (не хранится числом) — /undo
  автоматически пересчитывает её.
- **/undo всегда просит подтверждение кнопкой** и показывает, ЧТО будет
  отменено (после инцидента 21.07.2026: админ голым /undo в чате случайно
  откатил стартовую загрузку Каракола — операция №1; восстановили повторным
  /loadkarakol).
- Модель ИИ: **claude-sonnet-5** (переключается env CLAUDE_MODEL). Системный
  промпт разделён: статичный блок (правила+прайс, cache_control ephemeral)
  + динамичный (дата/сотрудник/склады) — НЕ вставлять динамику в статичную
  часть, это ломает кэш. История диалога — 10 сообщений.

## Большая ревизия кода 21.07.2026 (по просьбе владельца)

Три параллельных ревью (db, хендлеры, PDF/фон) + исправления, всё проверено
смоук-тестами. Ключевое, что теперь надо ЗНАТЬ:
- **Замена накладной (amend) атомарна**: db.replace_operation делает сторно
  старой и проведение новой одной транзакцией; проводится от имени АВТОРА
  старой накладной (op_user_id) — иначе приход «переезжал» в кассу админа.
- **Стартовый долг нового клиента идёт через журнал** (дельтой операции,
  клиент создаётся с debt=0) — /undo снимает и его; долг реконструируется
  из журнала.
- **/undo показывает предупреждение**, если сторно уведёт остатки в минус.
  /undo N сотруднику теперь явно запрещён (раньше N молча игнорировался).
- **Вечерние сводки и сводка черновиков — за скользящие сутки** (last_hours=24),
  а не «с полуночи»: раньше операции после часа отправки не попадали никуда.
- **Фоновые текстовые рассылки** (должники, мёртвый товар) режутся на куски
  ≤4000 символов (send_long_bot) — раньше длинные молча не доходили.
- /loadkarakol перепроверяет остатки на кнопке (двойная заявка не задвоит).
- При гонке «новый клиент появился, пока заявка ждала кнопки» накладная
  привязывается к существующему клиенту, а не падает по UNIQUE.
- Кэш системного промпта — ttl 1h (сообщения реже 5 мин; учёт в /api по 2x
  за запись). known_client_names кэшируется на 2 мин (known_clients_cached).
- Индексы на operations(client_id/user_id/ts) и draft_log(ts) — в SCHEMA.
- STT: бюджет подсказки 380 символов (лимит Whisper 224 токена), 429 от
  Groq — один повтор через 3 сек. В подсказку влезает ~22 первых слова
  препаратов — все не помещаются, это осознанный компромисс.
- Ошибка проведения заявки показывается и нажавшему кнопку (админу), и
  заявителю; «⛔ нет доступа» в группах не отвечается (тишина).
- ОСТОРОЖНО: вся конкурентность БД держится на одном asyncio-потоке и
  PENDING.pop до await; при переносе db-вызовов в executor проверки
  остатков надо переносить внутрь транзакций.
- Известные НЕ исправленные мелочи: сводки могут задвоиться при перекрытии
  деплоев в момент отправки; экспорт Excel включает отменённые операции в
  колонки сумм (помечены статусом), товар вне активного прайса с остатком
  в экспорт не попадает; уволенный сотрудник с несданной кассой не виден
  в экспорте.

## Текущее состояние: ПЕРЕХОДНЫЙ ПЕРИОД

- `TRANSITION_MODE` (env, по умолчанию **включён**): сотрудникам доступны
  только черновики и вопросы о прайсе; оплаты/перемещения/возвраты
  заблокированы (админу — нет). База наполняется.
- В переходном периоде **любая накладная — и сотрудника, и админа —
  автоматически становится черновиком**, слово «черновик» писать не нужно
  (сделано для выездной торговли; владелец сам попросил включить и для себя).
- **ИСКЛЮЧЕНИЕ — склады в полном режиме** (warehouses.full_mode=1,
  команда /fullmode): для них всё по-настоящему даже при TRANSITION_MODE=1.
  Первый такой склад — Каракол (с 21.07.2026).
- `DRAFT_WATERMARK` (по умолчанию **выключен**): черновики выглядят как
  обычные накладные (без водяного знака) — их отдают клиентам.
- **Когда владелец скажет «открываем полный учёт»**: поставить TRANSITION_MODE=0,
  DRAFT_WATERMARK=1, и обновить большую PDF-инструкцию (генератор —
  make_manual.py в скретчпаде старой сессии; проще перегенерировать заново)
  под все новые функции.

## Что реализовано (PR #1–#21, всё в main)

1. База: склады/роли/клиенты/журнал, миграции (#1, #2)
2. Черновики без водяного знака + переходный замок (#3, #4)
3. /report + вечерняя сводка в чаты в 20:00 (#5)
4. /olddebts + напоминания о должниках по понедельникам 10:00 (#6)
5. /client (карточка) + /act (акт сверки PDF) (#7)
6. Ежедневный бэкап базы админу в 03:00 + /backup (#8)
7. /minstock — минимальные остатки + предупреждения (#9)
8. Инвентаризация с подтверждением админа (#10)
9. Накладная по фото рукописного списка (Claude vision) (#11) —
   ЕЩЁ НЕ ПРОВЕРЕНА владельцем на живом фото
10. Спеццены клиентов (#12)
11. /pricepdf — фирменный PDF-прайс (#13)
12. Возврат товара с подтверждением админа (#14)
13. Касса сотрудников + сдача выручки /cash (#15)
14. /export — Excel (#16)
15. Телефоны контрагентов (#17)
16. /deadstock (мёртвый товар, автоотчёт 1-го числа 09:00) + /forecast (#18)
17. Экономия API: Haiku→Sonnet 5, кэширование промпта, история 10 (#19, #20)
18. /api + /apibalance — учёт расходов на API и остаток (#21)
19. Голосовые сообщения: речь→текст через Whisper (OpenAI-совместимый API,
    по умолчанию Groq whisper-large-v3 — бесплатно). Бот отвечает
    «🎤 Распознал: …» и дальше обрабатывает как обычный текст (включая
    «черновик» голосом). Whisper получает промпт-подсказку с названиями
    препаратов из прайса (укорочена — лимит Whisper 224 токена, длинная
    давала 400 от Groq). Владелец добавил GROQ_API_KEY на Railway и
    04.07.2026 подтвердил: голосовые распознаются. ✅
    Имена клиентов распознавались криво (жалоба владельца 10.07.2026) —
    теперь в подсказку Whisper добавляются имена известных клиентов
    (из clients + draft_log, частые первыми, бюджет ~420 символов), а в
    динамический блок промпта — список «Известные клиенты», чтобы ИИ
    исправлял ошибки распознавания на точные имена.
20. Сводка по черновикам: таблица draft_log (журнал черновиков, учёт не
    трогает), каждый вечер в 19:00 (DRAFT_SUMMARY_HOUR) админу в личку —
    кто сколько черновиков выписал и на какую сумму; если черновиков не
    было, сводка не шлётся. Команда /drafts — то же самое в любой момент.
21. Прайс в базе: таблицы products + price_history. Админ меняет цену
    фразой «новая цена Альтопен 100мл 95» (action change_price,
    подтверждение кнопкой), /pricelog — история изменений.
22. Обещания оплаты: таблица promises (клиент — текстом, не привязан к
    clients). «Асан обещал 50000 в пятницу» (action promise), «Асан
    выполнил обещание» (promise_done, закрывает все открытые по имени).
    /promises: админ видит все, сотрудник — свои. Утром в 09:00
    (PROMISE_HOUR) напоминание о сегодняшних и просроченных: админу всё,
    сотрудникам — их записи. Пишется без кнопки подтверждения (не учёт).
23. /abc — АВС-анализ (админ): товары по выручке группами A/B/C (80/15/5)
    и топ-10 клиентов. По реальным накладным; если их нет (переходный
    период) — по черновикам (draft_log.items хранит состав с суммами).
    Периоды как у /export: /abc неделя, /abc месяц, /abc все.
24. Справочник клиентов (админ): «добавь клиентов на Каракол: Асан,
    Болот...» — фразой/голосом/фото списка (action add_clients,
    подтверждение кнопкой, долг 0, дубли пропускаются). Имена сразу
    попадают в подсказку Whisper и список «Известные клиенты» промпта.
    В known_client_names приоритет: частые из draft_log, потом справочник.

Дополнения 22.07.2026: /clients [Склад] — полный справочник клиентов
склада PDF-файлом (алфавит, алиасы в скобках, долг/переплата/«—», телефон,
нумерация); отчёты /debts, /olddebts, /expiry получили колонку «№»
(опция numbered в generate_report_pdf); телефон клиента без указания
склада ищется по всем доступным складам (как псевдонимы). Каракол
перезагружен начисто через /resetwh: 29+1 клиентов (1'407'005 сом,
переплата Асель −1800, Туратбек уулу Акылбек с телефоном), остатки
6418 шт / 71 позиция (karakol_stock_data.py от 22.07.2026; срок у
ЭНРОТОП ФОРТЕ 1л не был виден на фото — оставлен 12.2027).

Ревизия безопасности 22.07.2026 (3 параллельных аудита: доступы,
сохранность базы, случайные изменения). Выводы аудитов: доступы крепкие
(незнакомец не может ничего; кнопки жмёт автор/админ; approver_id для
заявок; PENDING.pop до await; токены не логируются), бэкап через SQLite
backup API корректен при WAL, транзакции атомарны, миграции идемпотентны.
Исправлено по итогам:
- **Дедупликация заявок** (new_pending/_pending_key): одинаковое сообщение
  дважды (двойное касание/повтор голосового) больше не даёт две живые
  карточки — старая гаснет («Заявка устарела»), дубль операции невозможен.
- **/resetwh — двойное подтверждение**: после «Провести» вторая карточка
  «🗑 Да, стереть безвозвратно» (kind reset_wh2); перед стиранием
  автоматически шлётся бэкап базы админу, при неудаче бэкапа сброс
  отменяется.
- **Перемещение перепроверяет остаток на кнопке** (заявка могла ждать
  сутки): минус по-прежнему допустим, но подтверждающий видит предупреждение.
- **Обещания**: сотрудник закрывает «выполнил обещание» только СВОИ записи,
  админ — любые (раньше любой закрывал все по имени).
Известные осознанные решения (не баги): лента чата видна всем участникам
группы (по задумке); цены накладной фиксируются в момент карточки;
телефон клиента пишется без кнопки; админские /whname,/setwh,/feed и
т.п. одношаговые, но обратимые. reset_warehouse/backup держат event loop
синхронно — при больших объёмах перенести в executor.

Ревизия 27.07.2026 (3 аудита: расходы ИИ, качество, безопасность нового).
Исправлено:
- КРИТИЧНО, партии: план партий применяется один раз на (склад, товар)
  (_commit_op копирует и pop'ает), дельты накладной агрегируются по товару
  — товар двумя строками из двух партий списывался с задвоением;
  replace_operation принимает batch_plan (amend больше не теряет выбор).
- Сбой ПОСЛЕ коммита (Telegram/PDF) больше не показывает «Ошибка при
  проведении» — сообщение «ПРОВЕДЕНА, повторно не нужно, /op N»
  (_finish_invoice/_finish_transfer оборачивают пост-коммит в try).
  pbat-путь при ошибке уведомляет и заявителя.
- Кнопка партии кодирует СРОК (не индекс — список мог измениться),
  _ask_batch сохраняет TTL заявки (суточные заявки не сжимались до 15 мин).
- Дедуп заявок — только в окне 120 сек (две одинаковые сдачи утром и
  вечером — обе настоящие). commit_invoice: client_get None -> понятная
  ошибка.
- expiry_list: fallback на product_expiry только если у склада вообще нет
  партий (фантомные сроки после распродажи).
- ЭКОНОМИЯ ИИ: _request_history — окно истории всегда начинается с user
  (иначе 400 каждый второй запрос в длинном диалоге!) и старые сообщения
  режутся до HISTORY_MSG_MAX=700 симв (история давала ~46% цены запроса);
  при ошибке API user-сообщение убирается из истории (парность). Правила
  «известных клиентов» перенесены в статичный кэшируемый блок, в динамике
  только имена; имена в промпте сотрудника — только его склады
  (known_client_names(wh_ids), кэш по ключу складов). Фильтр чата:
  OP_STRONG/OP_WEAK (слабые слова требуют цифр), короткие приметы товаров
  («топ», «ивер») — только целым словом. Wake word: нечёткое совпадение
  только для слов на дж/ж/j.
- Безопасность: /clients,/invoices,/op,/stockat,/cash,/log — только личка
  (_require_private); handover из чата — только если склад чата доступен
  сотруднику; inspect_backup проверяет наличие НАШЕГО админа в файле;
  restore подчищает временные файлы (успех/отмена/ошибка); меню команд в
  группах скрыто.
Известные некритичные хвосты: stock_at не учитывает операции, отменённые позже
момента; FEFO-вопрос пропускается, если партий стало ≤1; явный срок
перемещения может увести партию источника в минус (задумано).
28.07.2026: возврат и инвентаризация дружат с партиями — явный срок
позиции («срок 11.2028») ложится своей партией, без срока — в ближайшую
датированную партию товара (_arrival_batch_plan), товара без партий —
в «без срока»; дельты возврата агрегируются по товару.

## Env-переменные (Railway)

TELEGRAM_TOKEN, ANTHROPIC_API_KEY — обязательные.
Опциональные: CLAUDE_MODEL (claude-sonnet-5), DB_PATH (/data/vetop.db),
TRANSITION_MODE (1), DRAFT_WATERMARK (0), SUMMARY_HOUR (20), BACKUP_HOUR (3),
DEBT_ALERT_DAYS (30), DEADSTOCK_DAYS (60), DRAFT_SUMMARY_HOUR (19),
PROMISE_HOUR (9).
Голосовые (STT): GROQ_API_KEY (рекомендуется, бесплатно) или OPENAI_API_KEY;
тонкая настройка — STT_API_KEY, STT_BASE_URL, STT_MODEL, STT_LANGUAGE
(ru; "auto" = автоопределение), MAX_VOICE_SECONDS (300).
Кыргызский язык: ELEVENLABS_API_KEY — если задан, голосовые идут через
ElevenLabs Scribe (ELEVENLABS_STT_MODEL, по умолчанию scribe_v1) с
автоопределением языка: русский и кыргызский, можно вперемешку. Whisper
кыргызский НЕ поддерживает (нет в его 99 языках). Подсказка с препаратами
и клиентами в ElevenLabs не передаётся — ошибки имён правит ИИ по списку
«Известные клиенты». Владельцу предложено зарегистрироваться на
elevenlabs.io (бесплатный тариф ограничен, дальше от ~$5/мес).

## Запуск Каракола в полном режиме (код готов, владелец проводит сам)

Владелец решил (20.07.2026): Каракол выходит из переходного периода и
работает по-настоящему, остальные склады пока на черновиках. Сделано:
- **full_mode по складам** (колонка в warehouses): /fullmode — статус,
  /fullmode Каракол — включить, /fullmode выкл Каракол — вернуть черновики.
  Для полного склада: накладные реальные (с кнопкой), оплаты/возвраты/
  перемещения/сдача выручки работают у сотрудников этого склада.
  Проверки идут по складу операции (перемещение — если любой из двух
  складов полный). «Черновик» словом остаётся доступен всегда.
- **/loadkarakol** (админ, одноразовая): стартовая загрузка остатков из
  karakol_stock_data.py (данные подтверждены владельцем: 71 позиция,
  6490 шт, просрочка как есть, Цефти DC 10 мл) — заявка с кнопкой,
  проводится операцией inventory; сроки годности пишутся в product_expiry.
  Повторная загрузка блокируется, если на складе уже есть остатки.
- **/expiry [Склад]** — сроки годности с пометками «скоро» (<3 мес) и
  «ПРОСРОЧЕНО»; таблица product_expiry(wh, product, expiry MM.YYYY) —
  один срок на товар (полноценные партии — отдельный план ниже).
- **Отчёты PDF-файлом** (просьба владельца 21.07.2026): /stock, /debts,
  /expiry, /report, /abc и /olddebts присылают фирменный PDF
  (generate_report_pdf в invoice_pdf.py — универсальные секции-таблицы)
  с короткой подписью-итогом; пустые данные — текст без файла.
  Вечерние сводки в чаты остались текстом (report_data — общие данные для
  текста и PDF). Еженедельное напоминание о должниках (понедельник 10:00)
  с 27.07.2026 тоже приходит PDF-файлом (overdue_report — общая сборка
  отчёта для /olddebts и send_debt_alerts; пусто — сообщения нет).
  /log (01.08.2026) — журнал операций тоже PDF-файлом: секции по дням,
  колонки «№ оп. / Время / Сотрудник / Тип / Операция», отменённые
  помечены словом ОТМЕНЕНА (эмодзи в шрифте PDF нет — LOG_TYPE_NAMES
  даёт словесные названия типов), подпись-итог со счётчиком отменённых.
  По умолчанию 20 операций, максимум поднят с 30 до 100 (/log 50) —
  ограничение длины сообщения в файле больше не действует.
  /report по ОДНОМУ складу (или у сотрудника с одним складом) показывает
  ВСЕ проданные препараты списком с нумерацией и подписью «Позиций
  продано: N» (просьба владельца 01.08.2026); общий отчёт по нескольким
  складам оставляет топ-10 на склад и пишет, сколько позиций скрыто —
  иначе PDF разрастается. Вечерняя сводка чата с одним складом тоже
  показывает всё проданное (тот же build_report_pdf).
  /stock идёт по ПОЛНОМУ прайсу (все 104 позиции, нулевые — прочерком,
  итог «В наличии: N из 104») — просьба владельца 21.07.2026: раньше
  нумерация с пропусками сбивала («потерялась» позиция №104).
  /stockprice (21.07.2026) — то же, но с колонками «Цена» и «Сумма»
  (продажные цены прайса) и итогом «на сколько сомов товара на складе».
  /stock и /stockprice без склада при нескольких доступных складах
  спрашивают кнопками, какой показать (kind pick_stock, есть кнопка
  «Все склады сразу»; /stock all — без вопросов). Остальные отчёты
  (/debts, /report, /olddebts, /expiry) без склада показывают все
  доступные склады секциями одного PDF.
  Отчёт по закупочным ценам сделан 02.08.2026 — /stockcost (см. раздел
  «Маржа по закупочным ценам»).
- **add_clients теперь с долгами**: clients: [{"name":..., "debt":...}] —
  «добавь клиентов на Каракол: Мустанг — 31470, ...»; существующим
  клиентам долг НЕ меняется, пропускаются.
Порядок запуска владельцем: /fullmode Каракол → /loadkarakol (кнопка) →
прислать боту клиентов с долгами → Данияр работает по-настоящему.
- **/resetwh Склад** (админ, 21.07.2026): ПОЛНЫЙ сброс склада — удаляет
  (не сторно) все операции журнала по складу (включая перемещения, где он
  одна из сторон — чужие дельты откатываются), остатки, сроки годности,
  всех клиентов с долгами (+ алиасы и спеццены). Кассы пересчитываются
  сами (журнальные). Карточка подтверждения показывает, что будет стёрто;
  /undo сброс не вернёт. Сделана по просьбе владельца: в день запуска
  Каракола были ошибочные заносы, он хотел стереть склад и загрузить
  заново (/resetwh Каракол → /loadkarakol → список клиентов с долгами;
  резерв списка — karakol_clients_message.txt, суммы владелец правит
  под актуальные).

Дополнительно (20.07.2026):
- Окно /undo для сотрудника увеличено с 15 минут до **1 часа**
  (env UNDO_MINUTES). Админ по-прежнему может отменить любую операцию.
- **Дополнение накладной** (action amend_invoice): «добавь в последнюю
  накладную Мустанга: Дексатоп 50мл 5 шт» — бот сторнирует последнюю
  накладную клиента и проводит новую (старые позиции + добавленные,
  цены старых сохраняются price_explicit, приход переносится), один
  итоговый PDF, пометка 🆕 у новых позиций. Сотрудник может заменить
  только СВОЮ накладную не старше часа; админ — любую. На черновичных
  складах — подсказка выписать черновик заново.

Дополнение 21.07.2026 (после запуска операций в чатах складов):
- **Бот должен быть АДМИНОМ в каждом групповом чате склада** — иначе
  Telegram (режим приватности) не передаёт ему обычные сообщения, и
  операции в чате молча не доходят (бот отвечает только на /команды).
  Каракол настроен 21.07.2026; при подключении других чатов — не забыть.
- **Операция за сотрудника**: админ пишет «проведи за Данияра: Валя приход
  1490» — ИИ добавляет поле as_employee, dispatch_action подменяет actor
  на сотрудника: операция в журнале от его имени, деньги в ЕГО кассу.
  Сотрудникам поле игнорируется. Правило: оплату проводит тот, у кого
  деньги физически на руках.
- **Псевдонимы клиентов** (таблица client_aliases): админ пишет «Вика
  Уманец — она же Виктория» (action client_alias, подтверждение кнопкой) —
  бот понимает псевдоним в оплатах/накладных (client_exact и fuzzy_clients
  ищут и по алиасам), имена попадают в подсказки ИИ и Whisper, /client
  показывает «Также известен как». Псевдоним, совпадающий с именем другого
  клиента склада, отклоняется. Первые алиасы Каракола: Вика Уманец
  (Виктория, Виктория Уманец), Валя (Валентина, Валя Липатова),
  Бейшембаева Гульнара Диана (Гульнара, Диана, Гульнара Бейшембаева),
  Асель Ананьево (Асель, Романова Асель) — владелец вводит сам.

## Партионный учёт — сроки годности (ЭТАП 1 СДЕЛАН 24.07.2026)

Запрос владельца 19.07.2026, «делай» получено 24.07.2026 (Excel Кара-Балты
с двумя сроками у товара). Сделано (этап 1):
- Таблица product_batches(wh, product, expiry '' = без срока, qty);
  UNIQUE(wh, product, expiry). Сумма партий = остатку склада (инвариант
  держит _apply_batches внутри _commit_op).
- Любое списание идёт FEFO («сначала старые», партия без срока — последней);
  приход без срока — в партию «без срока»; нехватка партий — минус в
  партии «без срока». Дельты партий пишутся в журнал (batch_deltas в data),
  /undo, замена накладной и reset_warehouse откатывают партии точно.
- Стартовая загрузка (load_wh) принимает несколько строк одного товара =
  партии (batch_plan в commit_operation); product_expiry заполняется
  ближайшим сроком (совместимость).
- /expiry: если у склада есть партии — каждая партия отдельной строкой.
- Миграция _migrate_batches (флаг batches_v1): существующие остатки
  складов становятся партиями со сроком из product_expiry.
Этап 2 (25.07.2026): при проведении НАКЛАДНОЙ бот спрашивает кнопками,
из какой партии продано — по каждой позиции, у товара которой ≥2 партий
с остатком (callback pbat, «Сначала старые» = FEFO); выбранная партия
списывается даже в минус (продавец знает, что продал её). _finish_invoice
— общий путь проведения. /expiry: партии товара в одной строке
(«12.2027 — 20 шт, 11.2028 — 23 шт»), статус по ближайшему сроку.
Миграция: сроки 19xx -> 20xx (год «30» из Excel парсился как 1930).
25.07.2026: перемещение тоже спрашивает партию (общий механизм
_batch_questions/_ask_batch, _batch_src_wh = источник) и ПЕРЕНОСИТ срок
выбранной/FEFO-партии на склад-получатель (src_batches в _finish_transfer
-> batch_plan обоих складов в commit_transfer); приход извне принимает
"expiry" у позиции («срок 11.2028», «11/28» — _norm_expiry), PDF
прихода/перемещения уходит в чаты-ленты складов.
Дальше (не сделано): выбор партии в замене накладной (amend списывает
FEFO), сроки в черновиках.

## Списание товара + обязательный срок (01.08.2026, просьба владельца)

Владелец: «при перемещении со склада на склад или при списании бот должен
спрашивать срок годности; без срока операцию не пропускать».
- **Новая операция «списание»** (action writeoff, тип операции writeoff):
  товар уходит со склада без клиента и без денег — просрочка, бой, порча,
  недостача, утилизация. «спиши Альтопен 100мл 5 шт — просрочка»,
  «списание Каракол: ... бой». Причина хранится в data.reason, сумма
  считается по прайсу. Проводит ТОЛЬКО админ (у сотрудника — заявка),
  на черновичных складах запрещено (как возврат/инвентаризация).
  Иконка 🗑, /undo возвращает товар и партии, есть в /op, /log, Excel.
- **Срок годности обязателен для перемещения и списания**
  (EXPIRY_REQUIRED_KINDS): если датированных партий на складе-источнике
  не хватает на списываемое количество, бот НЕ проводит операцию, а
  просит написать срок с упаковки текстом (_expiry_questions/_ask_expiry,
  AWAIT_EXPIRY по ключу (chat_id, user_id)). Ответ ловится ДО ИИ
  (expiry_reply в handle_message/handle_voice) — бесплатно и быстро;
  «11/28», «до 12.27» тоже понимаются (_norm_expiry). В группе непонятный
  ответ не перехватывается (вопрос просто висит), в личке бот подсказывает
  формат. Названный срок едет с товаром на склад-получатель, а на
  источнике списывается реальная партия «без срока» (флаг expiry_asked,
  _fill_src_batches) — иначе в источнике появилась бы минусовая партия.
- Кнопочный выбор партии зависит от операции (_batch_options): перемещение
  и списание показывают только ДАТИРОВАННЫЕ партии и только когда их
  хватает на объём (иначе спрашивается срок текстом), а НАКЛАДНАЯ —
  все партии склада, включая «без срока» (просьба владельца 01.08.2026:
  «при выписывании накладной, если у товара несколько сроков, спрашивать»).
  Кнопка «📦 Без срока» кодируется знаком BATCH_NO_EXPIRY="~" и списывает
  именно партию без срока: пустая строка в batch_choices означает FEFO,
  поэтому раньше выбор «без срока» уводил списание в старую датированную
  партию (_choice_batch переводит выбор в срок для плана). Сам срок
  годности продажа по-прежнему НЕ требует — чтобы не тормозить отпуск
  товара клиенту.
- Общий путь вопросов и проведения — _continue_op (кнопки → срок → провод),
  ответ текстом продолжает диалог через _MsgResponder.

Ревизия 01.08.2026 (Fable 5, 3 параллельных аудита нового кода сессии:
изменения, доступы, целостность учёта). Аудиты подтвердили: обхода
подтверждений админа нет, /undo и /resetwh корректны для writeoff, касса
списание не видит, транзакции/партии целы. Исправлено по итогам:
- «Отмена» на ЛЮБОЙ карточке снимала чужое ожидание срока (AWAIT_EXPIRY
  pop без сверки токена) — теперь снимается только своё.
- Второй вопрос о сроке затирал первый (один слот на (chat,user), у админа
  все заявки в одной личке; ответ уходил не в ту операцию) — теперь второй
  вопрос откладывается кнопкой «🔁 Продолжить», отвечать по одному.
- Протухший вопрос о сроке «съедал» следующее сообщение («Асан оплатил
  5000» → «заявка устарела», оплата терялась) — теперь предупреждение +
  сообщение идёт в обычную обработку.
- Ответ о сроке принимался слишком либерально («в 10.30 приеду» →
  срок 10.2030, операция проводилась) — теперь строгий EXPIRY_REPLY_RE
  (почти только дата, допускаются «до», «срок», «г.»).
- Списание не перепроверяло остаток на кнопке (заявка ждёт сутки) —
  теперь предупреждение о минусе, как у перемещения.
- Две FEFO-строки одного товара списывали одну партию дважды
  (_fill_src_batches: снимок партий без учёта соседних строк; инвариант
  не ломался, но /expiry врал) — теперь словарь used.
- /log -5 выгружал ВЕСЬ журнал (отрицательный LIMIT в SQLite = без
  лимита) — теперь max(1, ...).
- /debts и /olddebts закрыты в группах (_require_private — телефоны и
  долги по всем складам сотрудника; та же логика, что закрыла /clients).
- Excel: у списания появилась сумма (минусом, как у возврата).
- /report и вечерняя сводка показывают строку «Списания: N на X сом»
  (день с одним списанием больше не «день без операций»).
- expiry_reply проверяет активность сотрудника (уволенный между кнопкой
  и ответом не доведёт операцию).
- Вопрос о сроке при частично датированных партиях честно пишет
  «датированных хватает только на N шт из M» (названный срок ложится на
  ВЕСЬ объём получателя — осознанно).
Дополнение (просьба владельца 01.08.2026): вопрос о сроке по заявке
сотрудника уходит И САМОМУ сотруднику в его чат (упаковка у него на
складе) — AWAIT_EXPIRY держит два ключа (админ + заявитель) на один
токен, принимается первый ответ, второе ожидание снимается; отмена
админом снимает оба. Ответивший сотрудник виден админу уведомлением
«назвал срок X — провожу». Если сообщение сотруднику не доставилось
(бот заблокирован) — вопрос остаётся только у админа, как раньше.
Кнопка «Отмена» под вопросом показывается только тому, кто вправе её
нажать (у заявки — только админ).

## Замена накладной целиком + номер в PDF (02.08.2026, просьба владельца)

Запрос: Данияр хотел добавить товар в ПРЕДПОСЛЕДНЮЮ накладную и изменить
количество — «добавь в последнюю» этого не умело (только последняя
накладная клиента, только добавление). Сделано:
- **Действие replace_invoice** (РЕЖИМ 16 промпта): «замени накладную №45:
  Асан, Альтопен 100мл 10 шт, ...» — полный НОВЫЙ состав; так можно
  изменить количество и убрать товар. Без номера («замени накладную
  Асана: ...») берётся последняя накладная клиента. Права как у
  дополнения: сотрудник — только СВОЮ не старше часа (общий помощник
  _replace_rights_error), админ — любую; проводится от имени автора
  старой, приход старой накладной сохраняется. Карточка подтверждения и
  проведение — общие с amend (kind amend_invoice, full_replace=True,
  returned_qty — запас от сторно для проверки остатков; old_count=0).
  Партии списываются FEFO (кнопочного выбора при замене нет — как у
  amend). При наличии op_id вопрос «на каком складе?» и подстановка
  склада чата пропускаются (склад определяет сама накладная).
- **Номер накладной в PDF**: заголовок «НАКЛАДНАЯ № 47» (doc_number в
  generate_pdf_invoice; op_id прокидывается из _finish_invoice и замены,
  в личку и в ленту). По номеру накладную находят в /op, /log и заменяют.
  Черновики — без номера (они не операции журнала).
- **ФИКС учёта (был и в amend)**: если старая накладная создавала нового
  клиента со стартовым долгом, сторно при замене снимало и стартовый долг
  (он был дельтой той операции). Теперь commit_invoice переносит остаток
  дельты сверх «товар − оплата» в новую операцию — стартовый долг
  сохраняется, /undo новой накладной снимает и его.

## Ревизия экономии токенов 01.08.2026 (по просьбе владельца)

Аудит подтвердил: утечек нет. Проверено: статичный блок без динамики
(~18.7к символов, строится один раз); динамический блок мал (~400 символов
+ до 40 имён клиентов); история 10 сообщений со срезкой старых до 700
символов и первым user; бесплатные фильтры чатов складов до ИИ; фото в
историю идёт текстовым следом; вызовов Claude ровно два (ask_claude и
handle_photo), фоновые задачи ИИ не дёргают. Добавлен КОНТРОЛЬ, чтобы
будущие обновления не сломали экономию молча:
- **/api показывает здоровье кэша**: долю входных токенов из кэша за
  30 дней (db.api_cache_stats). ≥40% — «♻️ кэш работает», <40% при
  ≥20 запросах — «⚠️ покажите Джарвису». Здоровая норма ~80%.
- **Самопроверка при старте** (_static_prompt_problems в _post_init):
  статичный блок сверяется на детерминизм (два вызова — байт-в-байт)
  и отсутствие сегодняшней даты; проблема — тревога админу в личку
  (как тревога о временном хранилище).

## Кара-Балта ЗАПУЩЕНА в полном режиме ✅

Подтверждено скриншотом /fullmode 02.08.2026: Кара-Балта и Каракол —
🟢 полный учёт (с 05.08.2026 и Манас — см. раздел ниже), Бишкек —
черновики (переходный). Бека проводит реальные операции (приходы по
Кара-Балте видны в ленте). Владелец запускал сам; понедельничные PDF
о должниках и вечерние сводки по Кара-Балте работают автоматом.
НЕ предлагать владельцу «запустить Кара-Балту» — уже сделано.

История подготовки (24.07.2026): Excel остатков от 21.07.2026 зашит в
karabalta_stock_data.py: 104 товара, 70 с остатком, 73 партии, 4034 шт.
⚠️ ЭЛЕОВИТ 100мл: было расхождение (итог 19 / партия 29) — владелец
уточнил у Беки, исправлено на 19 (PR #108). Три товара двумя партиями:
АЛЬТОПЕН-ФОРТЕ 1л (12 шт 10.2026 + 1 шт ПРОСРОЧЕНА 09.2025), КЛОЗАТОП
500мл (23+20), АЛБЕНИВЕР 1л (8+24).

## Маржа по закупочным ценам (СДЕЛАНО 02.08.2026, только для админа)

Владелец прислал инвойс завода Hebei New Century Pharmaceutical
№ F260403 от 21.07.2026 (закупка на $124'298, CIP Аламедин, поездом) —
появились закупочные цены. Сделано:
- **products.buy_usd** (REAL, NULL = закуп неизвестен) — закупочная цена
  в ДОЛЛАРАХ за единицу прайса. Одноразовое сидирование из
  buy_prices_data.py (31 позиция, флаг buy_prices_f260403 в settings;
  db.seed_buy_prices вызывается ПОСЛЕ seed_products). Сверено с
  владельцем 02.08.2026: строка «10ml $0.17» после Мульти-Витатопа —
  это ОКСИТОП, наша единица «10 мл (10 фл)» = 10 флаконов → $1.70 (и у
  ЭНРОТОПА так же); «Альтопен 600мг box 7g» = упаковка 55 таб;
  «Compound Alben Ivermec» в инвойсе назван АЛЬТОПЕР — это №10–13
  прайса. ⚠️ АЛБЕНИВЕР (№76–79) — ДРУГОЙ товар: состав тот же, но завод
  и закупочные цены другие (подтвердил владелец 03.08.2026). Закуп
  Альтопера ему НЕ копировать. 03.08.2026 владелец прислал контракт
  ВТОРОГО завода (Shijiazhuang Shimu, TQ20251223C от 23.12.2025, CIF
  Аламедин, $143'968) — закуп Албенивера и линейки №69–101 заведён:
  BUY_USD_TQ20251223 в buy_prices_data.py (21 позиция, флаг
  buy_prices_tq20251223, seed_buy_prices принимает flag). Арифметика
  всех строк сверена с контрактом (qty × цена = сумма, итог совпал).
  Фасовки БЕЗ закупа (нет в контракте): Авертоп 500мл, Альбен Плюс
  100мл, Ивервит-Е 50/100мл, Иверклоз 50мл, Клозан Плюс 100мл.
- **Курс**: settings usd_rate (87.5 — задан владельцем 02.08.2026),
  меняется фразой «курс 88» (action set_usd_rate, только админ, без
  кнопки). **Накладные расходы** (таможня, доставка): settings
  buy_markup_pct (0), фраза «накладные расходы 10%» (set_buy_markup) —
  владелец обещал прислать процент после растаможки. Себестоимость в
  сомах = buy_usd × курс × (1 + %/100) — bot.buy_som_map().
- **«закупочная цена Дексатоп 50мл 0.47»** (action set_buy_price, РЕЖИМ
  17 промпта, USD по умолчанию, «41 сом» пересчитывается по курсу;
  карточка подтверждения kind set_buy).
- **/margin [день|неделя|месяц|все]** (админ, личка, по умолчанию месяц)
  — PDF прибыли: выручка по фактическим ценам накладных минус возвраты,
  себестоимость по закупу, наценка %, разбивка по складам, топ-15
  товаров по прибыли; списания — отдельной строкой потерь по закупу;
  продажи товаров без закупочной цены — отдельной строкой (в прибыль
  не входят, товары перечислены в подвале отчёта).
- **/stockcost [Склад|all]** — остатки в закупочных ценах + потенциальная
  наценка (закуп vs продажа). **/rate** — курс, расходы, сколько цен задано.
- ВАЖНО: закупочные цены НЕ попадают в промпт ИИ (секрет от сотрудников
  + экономия токенов) и не видны ни в одной команде сотрудников. Прибыль
  по старым операциям считается по ТЕКУЩЕМУ курсу (осознанное упрощение).

## Ночная сессия 02→03.08.2026 («потрать токены с пользой», владелец спал)

1. **tests.py В РЕПОЗИТОРИИ** — постоянные смоук-тесты (накладные, замена,
   партии FEFO, права, закуп/маржа, фильтры, дедуп, миграции). Запуск:
   python3 tests.py (временная база, API-ключи не нужны). ПРАВИЛО ПРОЕКТА:
   гонять перед КАЖДЫМ пушем; новая функция = новый сценарий в tests.py.
2. **Три параллельных аудита нового кода** (доступы, учёт, устойчивость).
   Исправлено по итогам:
   - карточка закупочных цен (set_buy) в групповом чате светила цены всем —
     теперь только личка; /rate тоже _require_private;
   - «добавь в накладную №45» МОЛЧА дополняло последнюю накладную клиента
     (номер терялся) — amend_invoice принимает op_id (общий помощник
     _invoice_by_op_id с заменой), режим 14 промпта обновлён;
   - op_id, случайно приехавший от модели в других действиях, отключал
     подстановку склада чата и вопрос «на каком складе?» — пропуск теперь
     только для amend/replace;
   - пост-коммит замены обёрнут try: сбой Telegram/PDF после commit больше
     не показывает «Ошибка» (сообщение «ПРОВЕДЕНА, /op N» — как в
     _finish_invoice);
   - права сотрудника перепроверяются на кнопке замены (карточка 15 мин
     растягивала часовое окно);
   - **_migrate_price_items** (флаг price_order_v2_items): перенумерация
     21.07 правила в журнале только stock_deltas, а items[].product_id
     оставался старым — /margin считал бы себестоимость старых операций
     по чужому товару. Чинится по имени+фасовке;
   - /margin: списания без закупочной цены видны («+N поз. без закупа»),
     период с одними списаниями не прячется за «нечего считать»;
   - /stockcost: минусовые остатки и товары вне прайса включены в суммы;
   - OverflowError/«№№45» при кривом номере накладной — вежливые ответы.
   Осознанно НЕ чинилось: замена старой накладной переносит её выручку в
   текущий период /margin (свойство сторно-модели); дедуп заявок слепнет
   после кнопки выбора клиента (окно 120 сек, риск мал).
3. **Сводки не задваиваются при перекрытии деплоев**: таблица job_log +
   db.claim_daily_job (атомарный INSERT) — каждую из 6 рассылок (вечерняя,
   черновики, обещания, должники, мёртвый товар, бэкап) шлёт ровно один
   экземпляр бота.
4. **Excel-экспорт починен**: отменённые операции без сумм в числовых
   колонках (итоги колонок сходятся), товар вне прайса с остатком виден,
   уволенный сотрудник с несданной кассой виден с пометкой «(уволен)».
5. **Инструкции** (генератор make_manual.py в скретчпаде сессии, PDF
   высланы владельцу в чат 03.08): большая инструкция manual_vetop.pdf
   (вся текущая функциональность, включая раздел владельца), памятки
   Данияру (Каракол) и Беке (Кара-Балта) с заменой накладной и номерами.

## Отчётные команды в чатах складов (03.08.2026, просьба владельца)

«Если в общем чате пишется команда про должников или склад — показывать
только этот склад, а не все». Сделано (_group_only_feed_whs): /stock,
/stockprice, /debts, /report, /expiry в ГРУППЕ показывают только
склад(ы), привязанные к этому чату (/feed), и только если автор имеет
к ним доступ; аргумент-склад и кнопки выбора в группе игнорируются
(период у /report работает). В группе без привязки или без доступа —
отказ «напишите в личку». /debts тем самым ОТКРЫТ в чатах складов
(раньше только личка) — но строго своим складом: команда склада видит
свои долги и телефоны, чужие склады в общий чат не попадают. /clients,
/invoices, /olddebts, /log и прочее — по-прежнему только личка.

## Возврат: срок годности обязателен (03.08.2026, выбор владельца)

Вопрос владельца «по какому сроку возвращается товар?» → выбран строгий
вариант «как у списания»: позиции возврата БЕЗ явного срока не проводятся —
бот спрашивает срок с упаковки текстом (та же машинерия _expiry_questions/
_ask_expiry/_continue_op; для kind return вопрос по КАЖДОЙ позиции без
expiry, партии склада не при чём). _batch_questions для возврата отключён
(товар приходит, а не уходит). Проведение вынесено в _finish_return
(пост-коммит обёрнут try — сбой PDF больше не выглядит «не проведено»).
Названный срок ложится своей партией (как раньше), фолбэк «в ближайшую
датированную» остался только для истории/undo старых операций. Вопрос по
заявке сотрудника уходит и сотруднику, и админу — как у списания.

## Учебный склад (03.08.2026)

Владелец запускал обучение Азамата: склад «Учебный» (/whadd + /fullmode
+ /access), полный прайс по 100 шт со сроками годности (15 товаров
двумя партиями 50+50, три просроченные партии, четыре «скоро»), десять
клиентов «Тест …» с долгами, группа «ВЕТОП. УЧЕБНЫЙ» привязана /feed.
Владельцу отправлены: тренажёр на 10 заданий (trener_azamat.pdf),
короткая памятка (instrukcia_azamat.pdf) — генераторы в скретчпаде
сессии. ✅ 05.08.2026 обучение завершено, склад стёрт владельцем
(/resetwh Учебный) ПЕРЕД запуском Манаса — учебные деньги в кассе
Азамата обнулились. Механика is_training_wh (имя с «Учеб») осталась
в коде — новый полигон можно завести в любой момент.

## Докрутка 03.08.2026 (владелец выбрал пункты 1,2,3,4,6 из предложенных)

1. **Понедельничная тревога о сроках** (send_expiry_alerts в
   weekly_debt_loop, claim expiry_alert:дата): админу PDF «Сроки на
   исходе» — просроченные партии («списать») и истекающие ≤3 мес
   («продавать первым»). Пусто — не шлётся. Учебные склады не тревожат.
2. **Учебные склады вне общих итогов** (is_training_wh — имя начинается
   с «Учеб»): /margin и месячный отчёт исключают их операции полностью
   (сноска в подвале), /report показывает секцию, но «Итого (без
   учебного склада)», сводный /stockcost пропускает (явно по имени —
   можно). Полигон можно держать постоянно.
3. **Выбор партии при замене/дополнении накладной**: kind amend_invoice
   идёт через _continue_op (кнопки «из какой партии?» как у накладной),
   проведение — _finish_amend. Проверки прав/остатков — на кнопке до
   вопросов.
4. **Месячный итог прибыли автоматом**: 1-го числа в 09:00 (в
   monthly_deadstock_loop, claim margin_month:YYYY-MM) — PDF /margin за
   прошлый календарный месяц. build_margin_report(start, end, label) —
   общая сборка с границами периода для /margin и автоотчёта.
6. **PDF-инструкции обновлены** (03.08): срок при возврате, команды в
   чатах складов, учебный склад, новые автоотчёты — высланы владельцу.

## Доступ-просмотр /watch (04.08.2026, для Беки-снабженца)

Владелец: «Бека проверяет чужие склады и досылает товар — должен видеть
должников и остатки, но не проводить там оплаты/накладные». Сделано:
- access.view_only (миграция ALTER TABLE). /watch Имя Склад — просмотр;
  /access — полный (повторный вызов меняет тип); /noaccess снимает любой.
- db: visible_warehouses(include_view=True) — для ОТЧЁТОВ (просмотр
  включён); operable_warehouses/can_use_warehouse — для ОПЕРАЦИЙ (просмотр
  НЕ включён); can_view_warehouse — проверка просмотра.
- Отчёты по праву просмотра: /stock(+кнопки), /stockprice, /debts,
  /invoices, /clients, /report, /expiry, /stockat, чаты складов
  (_group_only_feed_whs). Операции строго по operable: resolve_warehouse
  (отказ «доступ только на ПРОСМОТР»), кнопки pick_wh, замена по номеру,
  запись телефона, сдача выручки (full_mode по operable).
- Перемещения НА чужой склад доступа не требуют (как и раньше) — Бека
  досылает товар, админ подтверждает.
- /users показывает «+ доступ: …» и «+ просмотр: …» раздельно.
- Ещё в этой сессии: деплой больше не воскрешает /remove и /noaccess
  (db.init применяет STAFF только при создании записи; админу всегда
  role/active). Жуми в отпуске — /remove 607647629, вернуть /add.

## Манас ЗАПУЩЕН в полном режиме ✅ (05–06.08.2026)

Владелец провёл запуск сам, всё сверено и подтверждено скриншотами:
- **Остатки** из двух фото-скриншотов Excel зашиты в manas_stock_data.py:
  66 позиций с остатком, 69 партий, 5854 шт (лист 2 = 1884, лист 1 =
  3970 — сверено полистово). Загрузка: /loadwh Манас (STOCK_LOADS в
  bot.py — общий механизм, «манас» добавлен рядом с каракол/карабалта).
  Двойные партии: Дексатоп 100мл (160@11.2028 + 25@12.2027), Топзантел
  100мл (80 + 3@09.2025 ПРОСРОЧЕНА), Авертоп 200мл (100+88). Просрочка
  есть у Мульти-Витатоп 100мл, Топзантел 100мл, Энротоп 100/50мл.
- **Клиенты**: 50 шт из 5 скриншотов, отправлены владельцу одним
  сообщением «добавь клиентов на Манас: …» (41 должник, долги
  4'876'470 сом — сверено с эталоном до сома).
- **Проверка после загрузки**: /stockprice Манас = 2'182'835 сом,
  /stock и /debts совпали с исходными списками.
- Группа «ВЕТОП. ЮГ» привязана /feed, бот — админ чата.
- Порядок был: /resetwh Учебный → /fullmode Манас → /loadwh Манас →
  клиенты с долгами → /feed. НЕ предлагать «запустить Манас» — сделано.
- В переходном периоде (TRANSITION_MODE=1) остался ТОЛЬКО Бишкек.

**Схема доступов после запуска** (скриншот /users 06.08.2026):
- Абдурроууф — админ, всё.
- Азамат — Манас (полный).
- Данияр — Каракол (полный).
- Бека — Бишкек (родной) + доступ Кара-Балта (полный) + ПРОСМОТР
  Каракол и Манас (/watch — он снабженец, видит остатки/должников
  чужих складов, но операции там проводить не может; перемещения НА
  чужой склад работают как раньше).
- Жуми — в отпуске, /remove 607647629. Вернуть: /add 607647629 Жуми
  (+ /access Жуми Кара-Балта, если нужен второй склад).

## Мелочи сессии 04–06.08.2026

- **Лого на каждом PDF**: _report_brand в invoice_pdf.py — маленькое
  лого фермы (22мм) в правом верхнем углу каждой страницы + подвал
  «ОсОО «ВЕТОП» · +996 700 99 88 11 · +996 700 887 666 · vettop@inbox.ru»
  внизу. Включено во все отчётные PDF (generate_report_pdf через
  onFirstPage/onLaterPages); просьба владельца 05.08.2026.
- **Памятки Азамату на двух языках** (RU + кыргызский, отдельными PDF):
  короткие pamyatka_azamat_ru/kg.pdf (make_azamat_two_lang.py) и
  подробные instrukcia_azamat_ru/kg.pdf (make_azamat_full.py) —
  генераторы в скретчпаде сессии 04–06.08, высланы владельцу.
  В PDF нет эмодзи (DejaVu без глифов — квадратики), только текст.
- **Пуш-уведомления владельцу**: договорённость — по завершении долгих
  задач слать пуш «Готово: …» / «Нужен ваш ответ: …» (инструмент
  PushNotification). НОЧЬЮ НЕ СЛАТЬ — придерживать до утра: ночные
  пуши будили владельца через часы. Владелец сам добавил Claude в
  разрешённые в iOS-режиме «Сон», потом убрал — его настройка, бот и
  ассистент на неё не влияют.

## Подсказка при опечатке в команде (07.08.2026)

Владелец написал /infobd и /datadb вместо /dbinfo — бот молчал, выглядело
как поломка. Теперь неизвестная команда получает ответ «Возможно, вы имели
в виду /dbinfo?» (unknown_command в bot.py): difflib по списку
KNOWN_COMMANDS (собирается в main() из зарегистрированных CommandHandler —
новые команды подхватываются сами) + проверка перестановки букв.
Отвечает только активным сотрудникам; незнакомцам — тишина; в группах —
только если команда адресована боту явно (/команда@имябота), чтобы не
встревать в команды других ботов. Совсем непохожая команда — отсылка
к /start.

## Полная тишина посторонним (07.08.2026, решение владельца)

Вопрос владельца «можно ли, чтобы посторонний вообще не находил бота?»
Спрятать бота из поиска Telegram нельзя (боты публичны по природе),
поэтому выбрана тактика «молчащий бот не интересен»: get_actor больше
НЕ отвечает «⛔ У вас нет доступа» в личке — незнакомец не получает
НИЧЕГО ни в личке, ни в группе (тест test_stranger_full_silence).
Алерт «⛔ Нет доступа» на кнопках (on_callback) оставлен осознанно:
кнопки видны только внутри групп с карточками, посторонний из поиска
до них не доберётся, а уволенному сотруднику алерт объясняет ситуацию.
Владельцу также предложено при желании переименовать бота в нейтральное
имя через @BotFather (по слову «ВЕТОП» бот находится в поиске).

## /cash — PDF-файлом (07.08.2026, просьба владельца)

«При команде /cash отправь не список, а сразу PDF-файл». Сделано:
_send_cash_pdf — касса PDF в фирменном стиле (generate_report_pdf):
секция движений на сотрудника (Дата/Сумма/Операция, последнее внизу,
итог «Сейчас на руках»), «Все сразу» — сводная таблица «Наличные на
руках» + секции движений всех, у кого есть касса или движения.
/cash Данияр — сразу PDF без кнопок (_match_employee: точное имя,
подстрока, difflib ≥0.7; неоднозначно/не найдено — кнопки как раньше);
/cash все|всех|all — общий PDF. Кнопки выбора остались, ответ кнопки —
тоже PDF. Пустая касса без движений — текст без файла (как у всех
отчётов). Ответ сотруднику на его /cash не менялся (краткий текст
с подсказкой «сдал N»).

## Переезд группы в супергруппу больше не рвёт ленту (07.08.2026)

Скриншот владельца: в чате «ВЕТОП. ЮГ» Бека получил на /stock отказ
«склад не привязан» — хотя Манас был привязан /feed, а у Беки есть
просмотр. Вероятная причина: Telegram сменил id чата при миграции
группы в супергруппу, привязка /feed молча слетела (лента и команды
чата перестают работать). Теперь on_chat_migrated (StatusUpdate.MIGRATE)
переносит feed_chat_id на новый id автоматически (db.migrate_feed_chat)
и сообщает админу в личку. Быстрая проверка привязки: /feed без
аргументов в самом чате (покажет привязанные склады); починка руками —
заново /feed Манас в чате.

- Накладные расходы на закуп: владелец пришлёт процент после растаможки
  (фраза «накладные расходы N%» уже работает).
- Заместитель на время отпуска (/deputy — передача права подтверждений)
  — владелец отложил 03.08.2026 («пятый пока отпусти»).
- Обновление большой PDF-инструкции после выхода из переходного периода.

## Как тестировать

API-ключей в окружении разработки нет — тестировать смоук-скриптами в
скретчпаде: подменить DB_PATH, задать dummy TELEGRAM_TOKEN/ANTHROPIC_API_KEY,
импортировать db/bot и проверять чистые функции и операции базы. PDF
генерировать и просматривать через Read. Перед пушем: py_compile всех файлов.

## Документы, отправленные владельцу (перегенерировать по запросу)

- Большая PDF-инструкция по боту (устарела — до складов/заявок/кассы).
- Памятка «черновик» для сотрудников на переходный период (актуальна).
- Памятка для Данияра — склад Каракол в полном учёте (21.07.2026,
  генератор make_karakol_manual.py в скретчпаде той сессии): накладные,
  оплаты, /undo, сдача выручки, заявки через админа, команды, правила.
- Памятки и инструкции Азамату RU+KG (04–06.08.2026): pamyatka_azamat_*
  и instrukcia_azamat_* — см. «Мелочи сессии 04–06.08.2026».
- Тренажёр trener_azamat.pdf и памятка учебного склада (03.08.2026).
